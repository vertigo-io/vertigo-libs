package io.vertigo.struts2.data.domain;

import java.util.Arrays;
import java.util.Iterator;

import io.vertigo.core.lang.Generated;
import io.vertigo.dynamo.domain.metamodel.DtFieldName;

/**
 * This class is automatically generated.
 * DO NOT EDIT THIS FILE DIRECTLY.
 */
@Generated
public final class DtDefinitions implements Iterable<Class<?>> {

	/**
	 * Enumération des DtDefinitions.
	 */
	public enum Definitions {
		/** Objet de données ApplicationUser. */
		ApplicationUser(io.vertigo.struts2.data.domain.users.ApplicationUser.class),
		/** Objet de données Casting. */
		Casting(io.vertigo.struts2.data.domain.people.Casting.class),
		/** Objet de données Commune. */
		Commune(io.vertigo.struts2.data.domain.reference.Commune.class),
		/** Objet de données Movie. */
		Movie(io.vertigo.struts2.data.domain.movies.Movie.class),
		/** Objet de données MovieDisplay. */
		MovieDisplay(io.vertigo.struts2.data.domain.movies.MovieDisplay.class),
		/** Objet de données MovieIndex. */
		MovieIndex(io.vertigo.struts2.data.domain.movies.MovieIndex.class),
		/** Objet de données OuiNonChoice. */
		OuiNonChoice(io.vertigo.struts2.data.domain.reference.OuiNonChoice.class),
		/** Objet de données People. */
		People(io.vertigo.struts2.data.domain.people.People.class),
		/** Objet de données Profil. */
		Profil(io.vertigo.struts2.data.domain.users.Profil.class),
		/** Objet de données SecurityRole. */
		SecurityRole(io.vertigo.struts2.data.domain.users.SecurityRole.class),
		/** Objet de données UserAuthentification. */
		UserAuthentification(io.vertigo.struts2.data.domain.users.UserAuthentification.class);

		private final Class<?> clazz;

		private Definitions(final Class<?> clazz) {
			this.clazz = clazz;
		}

		/**
		 * Classe associée.
		 * @return Class d'implémentation de l'objet
		 */
		public Class<?> getDtClass() {
			return clazz;
		}
	}

	/**
	 * Enumération des champs de ApplicationUser.
	 */
	public enum ApplicationUserFields implements DtFieldName<io.vertigo.struts2.data.domain.users.ApplicationUser> {
		/** Propriété 'USR_ID'. */
		usrId,
		/** Propriété 'Last Name'. */
		lastName,
		/** Propriété 'First Name'. */
		firstName,
		/** Propriété 'email'. */
		email,
		/** Propriété 'Profil'. */
		proId
	}

	/**
	 * Enumération des champs de Casting.
	 */
	public enum CastingFields implements DtFieldName<io.vertigo.struts2.data.domain.people.Casting> {
		/** Propriété 'Cast_id'. */
		castId,
		/** Propriété 'Character name'. */
		characterName,
		/** Propriété 'People'. */
		peoId,
		/** Propriété 'Movie'. */
		movId
	}

	/**
	 * Enumération des champs de Commune.
	 */
	public enum CommuneFields implements DtFieldName<io.vertigo.struts2.data.domain.reference.Commune> {
		/** Propriété 'idInsee'. */
		idInsee,
		/** Propriété 'codePostal'. */
		codePostal,
		/** Propriété 'commune'. */
		commune,
		/** Propriété 'departement'. */
		departement
	}

	/**
	 * Enumération des champs de Movie.
	 */
	public enum MovieFields implements DtFieldName<io.vertigo.struts2.data.domain.movies.Movie> {
		/** Propriété 'MOV_ID'. */
		movId,
		/** Propriété 'TITLE'. */
		title,
		/** Propriété 'Released'. */
		released,
		/** Propriété 'Year'. */
		year,
		/** Propriété 'Runtime'. */
		runtime,
		/** Propriété 'Description'. */
		description,
		/** Propriété 'Poster'. */
		poster,
		/** Propriété 'rated'. */
		rated,
		/** Propriété 'lastModified'. */
		lastModified
	}

	/**
	 * Enumération des champs de MovieDisplay.
	 */
	public enum MovieDisplayFields implements DtFieldName<io.vertigo.struts2.data.domain.movies.MovieDisplay> {
		/** Propriété 'id'. */
		movId,
		/** Propriété 'Title'. */
		title
	}

	/**
	 * Enumération des champs de MovieIndex.
	 */
	public enum MovieIndexFields implements DtFieldName<io.vertigo.struts2.data.domain.movies.MovieIndex> {
		/** Propriété 'id'. */
		movId,
		/** Propriété 'Title'. */
		title,
		/** Propriété 'label'. */
		titleSortOnly,
		/** Propriété 'Original Title'. */
		originalTitle,
		/** Propriété 'Synopsis'. */
		synopsis,
		/** Propriété 'Synopsis'. */
		shortSynopsis,
		/** Propriété 'keywords'. */
		keywords,
		/** Propriété 'poster'. */
		poster,
		/** Propriété 'runtime'. */
		runtime,
		/** Propriété 'Movie type'. */
		movieType,
		/** Propriété 'productionYear'. */
		productionYear,
		/** Propriété 'userRating'. */
		userRating,
		/** Propriété 'pressRating'. */
		pressRating,
		/** Propriété 'Camera'. */
		camera
	}

	/**
	 * Enumération des champs de OuiNonChoice.
	 */
	public enum OuiNonChoiceFields implements DtFieldName<io.vertigo.struts2.data.domain.reference.OuiNonChoice> {
		/** Propriété 'Valeur'. */
		key,
		/** Propriété 'Libellé'. */
		libelle
	}

	/**
	 * Enumération des champs de People.
	 */
	public enum PeopleFields implements DtFieldName<io.vertigo.struts2.data.domain.people.People> {
		/** Propriété 'PEO_ID'. */
		peoId,
		/** Propriété 'Last Name'. */
		lastName,
		/** Propriété 'First Name'. */
		firstName,
		/** Propriété 'Peo Name'. */
		peoName,
		/** Propriété 'imdbID'. */
		imdbid
	}

	/**
	 * Enumération des champs de Profil.
	 */
	public enum ProfilFields implements DtFieldName<io.vertigo.struts2.data.domain.users.Profil> {
		/** Propriété 'PRO_ID'. */
		proId,
		/** Propriété 'Label'. */
		label
	}

	/**
	 * Enumération des champs de SecurityRole.
	 */
	public enum SecurityRoleFields implements DtFieldName<io.vertigo.struts2.data.domain.users.SecurityRole> {
		/** Propriété 'SRO_CD'. */
		sroCd,
		/** Propriété 'Label'. */
		label
	}

	/**
	 * Enumération des champs de UserAuthentification.
	 */
	public enum UserAuthentificationFields implements DtFieldName<io.vertigo.struts2.data.domain.users.UserAuthentification> {
		/** Propriété 'AUTH_ID'. */
		authId,
		/** Propriété 'Login'. */
		login,
		/** Propriété 'Password'. */
		password,
		/** Propriété 'Application user'. */
		usrId
	}

	/** {@inheritDoc} */
	@Override
	public Iterator<Class<?>> iterator() {
		return new Iterator<Class<?>>() {
			private final Iterator<Definitions> it = Arrays.asList(Definitions.values()).iterator();

			/** {@inheritDoc} */
			@Override
			public boolean hasNext() {
				return it.hasNext();
			}

			/** {@inheritDoc} */
			@Override
			public Class<?> next() {
				return it.next().getDtClass();
			}
		};
	}
}
