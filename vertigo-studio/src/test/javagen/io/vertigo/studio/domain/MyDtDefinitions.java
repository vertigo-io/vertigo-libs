package io.vertigo.studio.domain;

import java.util.Arrays;
import java.util.Iterator;

import io.vertigo.core.lang.Generated;
import io.vertigo.datamodel.structure.metamodel.DtFieldName;

/**
 * This class is automatically generated.
 * DO NOT EDIT THIS FILE DIRECTLY.
 */
@Generated
public final class MyDtDefinitions implements Iterable<Class<?>> {

	/**
	 * Enumération des DtDefinitions.
	 */
	public enum Definitions {
		/** Objet de données Attachment. */
		Attachment(io.vertigo.studio.domain.stereotype.Attachment.class),
		/** Objet de données Car. */
		Car(io.vertigo.studio.domain.car.Car.class),
		/** Objet de données City. */
		City(io.vertigo.studio.domain.stereotype.City.class),
		/** Objet de données Command. */
		Command(io.vertigo.studio.domain.stereotype.Command.class),
		/** Objet de données CommandType. */
		CommandType(io.vertigo.studio.domain.stereotype.CommandType.class),
		/** Objet de données CommandValidation. */
		CommandValidation(io.vertigo.studio.domain.stereotype.CommandValidation.class),
		/** Objet de données Famille. */
		Famille(io.vertigo.studio.domain.famille.Famille.class),
		/** Objet de données MotorType. */
		MotorType(io.vertigo.studio.domain.car.MotorType.class),
		/** Objet de données Record. */
		Record(io.vertigo.studio.domain.security.Record.class),
		/** Objet de données VxFileInfo. */
		VxFileInfo(io.vertigo.studio.domain.fileinfo.VxFileInfo.class)		;

		private final Class<?> clazz;

		private Definitions(final Class<?> clazz) {
			this.clazz = clazz;
		}

		/** 
		 * Classe associée.
		 * @return Class d'implémentation de l'objet 
		 */
		public Class<?> getDtClass() {
			return clazz;
		}
	}

	/**
	 * Enumération des champs de Attachment.
	 */
	public enum AttachmentFields implements DtFieldName<io.vertigo.studio.domain.stereotype.Attachment> {
		/** Propriété 'id'. */
		attId,
		/** Propriété 'Url'. */
		url,
		/** Propriété 'Command'. */
		cmdId	}

	/**
	 * Enumération des champs de Car.
	 */
	public enum CarFields implements DtFieldName<io.vertigo.studio.domain.car.Car> {
		/** Propriété 'identifiant de la voiture'. */
		id,
		/** Propriété 'Constructeur'. */
		manufacturer,
		/** Propriété 'Modèle'. */
		model,
		/** Propriété 'Descriptif'. */
		description,
		/** Propriété 'Année'. */
		year,
		/** Propriété 'Kilométrage'. */
		kilo,
		/** Propriété 'Prix'. */
		price,
		/** Propriété 'Consommation'. */
		consommation,
		/** Propriété 'Motor type'. */
		mtyCd,
		/** Propriété 'Famille'. */
		famId	}

	/**
	 * Enumération des champs de City.
	 */
	public enum CityFields implements DtFieldName<io.vertigo.studio.domain.stereotype.City> {
		/** Propriété 'id'. */
		citId,
		/** Propriété 'Label'. */
		label,
		/** Propriété 'Postal code'. */
		postalCode	}

	/**
	 * Enumération des champs de Command.
	 */
	public enum CommandFields implements DtFieldName<io.vertigo.studio.domain.stereotype.Command> {
		/** Propriété 'id'. */
		cmdId,
		/** Propriété 'Command type'. */
		ctyId,
		/** Propriété 'City'. */
		citId	}

	/**
	 * Enumération des champs de CommandType.
	 */
	public enum CommandTypeFields implements DtFieldName<io.vertigo.studio.domain.stereotype.CommandType> {
		/** Propriété 'id'. */
		ctyId,
		/** Propriété 'Label'. */
		label	}

	/**
	 * Enumération des champs de CommandValidation.
	 */
	public enum CommandValidationFields implements DtFieldName<io.vertigo.studio.domain.stereotype.CommandValidation> {
		/** Propriété 'id'. */
		cvaId,
		/** Propriété 'Signer name'. */
		signerName,
		/** Propriété 'Command'. */
		cmdId	}

	/**
	 * Enumération des champs de Famille.
	 */
	public enum FamilleFields implements DtFieldName<io.vertigo.studio.domain.famille.Famille> {
		/** Propriété 'identifiant de la famille'. */
		famId,
		/** Propriété 'Libelle'. */
		libelle,
		/** Propriété 'Libelle'. */
		description	}

	/**
	 * Enumération des champs de MotorType.
	 */
	public enum MotorTypeFields implements DtFieldName<io.vertigo.studio.domain.car.MotorType> {
		/** Propriété 'id'. */
		mtyCd,
		/** Propriété 'Label'. */
		label	}

	/**
	 * Enumération des champs de Record.
	 */
	public enum RecordFields implements DtFieldName<io.vertigo.studio.domain.security.Record> {
		/** Propriété 'Id'. */
		dosId,
		/** Propriété 'Region'. */
		regId,
		/** Propriété 'Departement'. */
		depId,
		/** Propriété 'Commune'. */
		comId,
		/** Propriété 'Record type'. */
		typId,
		/** Propriété 'Title'. */
		title,
		/** Propriété 'Amount'. */
		amount,
		/** Propriété 'Owner'. */
		utiIdOwner,
		/** Propriété 'State'. */
		etaCd	}

	/**
	 * Enumération des champs de VxFileInfo.
	 */
	public enum VxFileInfoFields implements DtFieldName<io.vertigo.studio.domain.fileinfo.VxFileInfo> {
		/** Propriété 'Identifiant'. */
		filId,
		/** Propriété 'Nom'. */
		fileName,
		/** Propriété 'Type mime'. */
		mimeType,
		/** Propriété 'Taille'. */
		length,
		/** Propriété 'Date de dernière modification'. */
		lastModified,
		/** Propriété 'data'. */
		fileData	}

	/** {@inheritDoc} */
	@Override
	public Iterator<Class<?>> iterator() {
		return new Iterator<Class<?>>() {
			private Iterator<Definitions> it = Arrays.asList(Definitions.values()).iterator();

			/** {@inheritDoc} */
			@Override
			public boolean hasNext() {
				return it.hasNext();
			}

			/** {@inheritDoc} */
			@Override
			public Class<?> next() {
				return it.next().getDtClass();
			}
		};
	}
}
